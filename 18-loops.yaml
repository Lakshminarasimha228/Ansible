- name: install packages 
  hosts: frontend
  tasks:
  - name: install packages
    ansible.builtin.debug:
      name: "{{ items }}"
      state: installed
    loop:
    - mysql  
    - nginx
    - zip
    - Python3
